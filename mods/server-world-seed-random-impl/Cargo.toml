[package]
name = "server-world-seed-random-impl"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "ServerWorldSeedRandomImpl"
provides = "server-world-seed-api"

[package.metadata.mod.dependencies]
init = ["bevy-mod"]
run = []
ownership = []

[dependencies]
bevy = "0.17.3"
bevy-mod = { path = "../bevy-mod" }
server-world-seed-api = { path = "../server-world-seed-api" }
tokio = { version = "1.48.0", features = ["full"] }
