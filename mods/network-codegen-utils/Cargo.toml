[package]
name = "network-codegen-utils"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
support = true

[dependencies]
codegen-utils = { git = "https://github.com/paolobettelini/modularis.git" }
toml = "0.9"
