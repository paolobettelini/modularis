[package]
name = "server-command-flight-speed-vanilla-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "ServerCommandFlightSpeedVanillaMod"

[package.metadata.mod.dependencies]
init = [
    "bevy-mod",
    "server-command-api",
    "server-chat-api",
    "server-player-registry-api",
    "server-player-flight-speed-api",
]
run = []
ownership = []

[dependencies]
audience-api = { path = "../audience-api" }
bevy = "0.17.3"
bevy-mod = { path = "../bevy-mod" }
player-network-message-types = { path = "../player-network-message-types" }
server-chat-api = { path = "../server-chat-api" }
server-command-api = { path = "../server-command-api" }
server-player-flight-speed-api = { path = "../server-player-flight-speed-api" }
server-player-registry-api = { path = "../server-player-registry-api" }
tokio = { version = "1.48.0", features = ["full"] }
