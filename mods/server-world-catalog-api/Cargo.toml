[package]
name = "server-world-catalog-api"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
support = true

[dependencies]
bevy = "0.17.3"
world-instance-api = { path = "../world-instance-api" }
