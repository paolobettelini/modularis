[package]
name = "player-speed-network-messages-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "PlayerSpeedNetworkMessagesMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.network.messages]
clientbound = ["player_speed_network_message_types::PlayerSpeedChanged"]
serverbound = []

[dependencies]
player-speed-network-message-types = { path = "../player-speed-network-message-types" }
tokio = { version = "1.48.0", features = ["full"] }
