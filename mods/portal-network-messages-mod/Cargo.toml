[package]
name = "portal-network-messages-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "PortalNetworkMessagesMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.network.messages]
clientbound = ["portal_network_message_types::PortalOpenedPacket"]
serverbound = []

[dependencies]
portal-network-message-types = { path = "../portal-network-message-types" }
tokio = { version = "1.48.0", features = ["full"] }
