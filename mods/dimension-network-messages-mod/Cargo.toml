[package]
name = "dimension-network-messages-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "DimensionNetworkMessagesMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.network.messages]
clientbound = ["dimension_network_message_types::PlayerDimensionChanged"]
serverbound = []

[dependencies]
dimension-network-message-types = { path = "../dimension-network-message-types" }
tokio = { version = "1.48.0", features = ["full"] }
