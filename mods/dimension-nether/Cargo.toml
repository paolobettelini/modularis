[package]
name = "dimension-nether"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "DimensionNetherMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.dimension]
id = "demo:nether"

[dependencies]
tokio = { version = "1.48.0", features = ["full"] }
