[package]
name = "server-bevy-log-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "ServerBevyLogMod"

[package.metadata.mod.dependencies]
init = ["bevy-mod"]
run = []
ownership = []

[dependencies]
bevy = "0.17.3"
bevy-mod = { path = "../bevy-mod" }
tokio = { version = "1.48.0", features = ["full"] }
