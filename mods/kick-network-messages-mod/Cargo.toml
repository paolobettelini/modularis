[package]
name = "kick-network-messages-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "KickNetworkMessagesMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.network.messages]
clientbound = ["kick_network_message_types::Kick"]
serverbound = []

[dependencies]
kick-network-message-types = { path = "../kick-network-message-types" }
tokio = { version = "1.48.0", features = ["full"] }
