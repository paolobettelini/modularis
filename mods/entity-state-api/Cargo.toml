[package]
name = "entity-state-api"
version = "0.1.0"
edition = "2024"

[dependencies]
generated-entity-registry = "0.1.0"
serde = {version="1",features=["derive"]}
uuid = {version="1",features=["v4","serde"]}

[package.metadata.mod]
support = true
