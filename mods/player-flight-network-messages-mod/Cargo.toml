[package]
name = "player-flight-network-messages-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "PlayerFlightNetworkMessagesMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.network.messages]
clientbound = ["player_flight_network_message_types::PlayerFlightCapabilityChanged"]
serverbound = []

[dependencies]
player-flight-network-message-types = { path = "../player-flight-network-message-types" }
tokio = { version = "1.48.0", features = ["full"] }
