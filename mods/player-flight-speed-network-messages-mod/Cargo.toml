[package]
name = "player-flight-speed-network-messages-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "PlayerFlightSpeedNetworkMessagesMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.network.messages]
clientbound = ["player_flight_speed_network_message_types::PlayerFlightSpeedChanged"]
serverbound = []

[dependencies]
player-flight-speed-network-message-types = { git = "https://github.com/paolobettelini/modularis.git" }
tokio = { version = "1.48.0", features = ["full"] }
