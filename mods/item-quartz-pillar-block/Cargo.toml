[package]
name = "item-quartz-pillar-block"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "ItemQuartzPillarBlockMod"

[package.metadata.mod.dependencies]
init = ["block-quartz-pillar"]
run = []
ownership = []

[package.metadata.item]
id = "demo:quartz_pillar_block"
label = "Quartz Pillar"

[dependencies]
item-api = { git = "https://github.com/paolobettelini/modularis.git" }
item-render-api = { git = "https://github.com/paolobettelini/modularis.git" }
block-quartz-pillar = { path = "../block-quartz-pillar" }
tokio = { version = "1.48.0", features = ["full"] }
