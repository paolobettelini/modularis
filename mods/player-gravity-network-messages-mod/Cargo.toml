[package]
name = "player-gravity-network-messages-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "PlayerGravityNetworkMessagesMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.network.messages]
clientbound = ["player_gravity_network_message_types::PlayerGravityChanged"]
serverbound = []

[dependencies]
player-gravity-network-message-types = { git = "https://github.com/paolobettelini/modularis.git" }
tokio = { version = "1.48.0", features = ["full"] }
