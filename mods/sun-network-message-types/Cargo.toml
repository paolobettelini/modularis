[package]
name = "sun-network-message-types"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sun-api = { git = "https://github.com/paolobettelini/modularis.git" }

