[package]
name = "chat-clear-network-messages-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "ChatClearNetworkMessagesMod"

[package.metadata.mod.dependencies]
init = []
run = []
ownership = []

[package.metadata.network.messages]
clientbound = ["chat_clear_network_message_types::ClearChat"]
serverbound = []

[dependencies]
chat-clear-network-message-types = { path = "../chat-clear-network-message-types" }
tokio = { version = "1.48.0", features = ["full"] }
