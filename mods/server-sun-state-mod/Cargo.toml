[package]
name = "server-sun-state-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "ServerSunStateMod"
provides = "server-sun-api"

[package.metadata.mod.dependencies]
init = ["bevy-mod"]
run = []
ownership = []

[dependencies]
bevy = "0.17.3"
bevy-mod = { path = "../bevy-mod" }
server-sun-api = { path = "../server-sun-api" }
tokio = { version = "1.48.0", features = ["full"] }

