[package]
name = "chunk-network-message-types"
version = "0.1.0"
edition = "2024"

[dependencies]
chunk-api = { path = "../chunk-api" }
serde = { version = "1.0", features = ["derive"] }
voxel-math-api = { path = "../voxel-math-api" }
