[package]
name = "entity-network-message-types"
version = "0.1.0"
edition = "2024"

[dependencies]
entity-state-api = { path = "../entity-state-api" }
serde = {version="1",features=["derive"]}

[package.metadata.mod]
support = true
