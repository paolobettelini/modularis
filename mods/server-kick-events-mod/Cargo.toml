[package]
name = "server-kick-events-mod"
version = "0.1.0"
edition = "2024"

[package.metadata.mod]
entry = "ServerKickEventsMod"
provides = "server-kick-api"

[package.metadata.mod.dependencies]
init = ["bevy-mod"]
run = []
ownership = []

[dependencies]
bevy = "0.17.3"
bevy-mod = { path = "../bevy-mod" }
server-kick-api = { path = "../server-kick-api" }
tokio = { version = "1.48.0", features = ["full"] }
